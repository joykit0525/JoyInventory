# JoyInventory 다른 기기 접속 방법

## 같은 와이파이/사무실 네트워크

1. 재고 서버 PC에서 아래 명령을 실행합니다.

```bash
npm start
```

2. 실행 화면에 표시되는 `Network access` 주소를 회사 컴퓨터, 노트북, 모바일 브라우저에서 엽니다.

```text
http://121.137.137.203:3000
```

3. Windows 방화벽 알림이 뜨면 Node.js의 개인 네트워크 접근을 허용합니다.

## 접속이 안 될 때

- 서버 PC와 접속 기기가 같은 와이파이 또는 같은 공유기 안에 있는지 확인합니다.
- 서버 PC가 절전 모드로 들어가면 접속할 수 없습니다.
- 주소가 바뀌면 `npm start`를 다시 실행해서 새 `Network access` 주소를 확인합니다.
- 회사 밖, 모바일 데이터, 다른 장소에서 접속하려면 이 서버를 클라우드/VPS/NAS에 배포하거나 Tailscale 같은 VPN/터널을 사용해야 합니다.

## 앱 주소

다른 기기에서는 `index.html` 파일을 직접 열지 말고 서버 주소로 접속하세요.

```text
http://서버PC주소:3000
```
